package com.citiustech.hospitalproject.service;

import org.springframework.stereotype.Service;

@Service
public class SchedularServiceImpl implements SchedularService{
	/*
	 * @Autowired private SchedulingRepo schedulerRepo;
	 * 
	 * @Override public List<Scheduling> getAppointments(LocalDate ld1) { // TODO
	 * Auto-generated method stub Date date =
	 * Date.from(ld1.atStartOfDay(ZoneId.systemDefault()).toInstant()); return
	 * schedulerRepo.findByAppointmentDateContaining(date.getTime()); }
	 */
}
